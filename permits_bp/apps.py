from django.apps import AppConfig


class PermitsBPConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'permits_bp'
    verbose_name = "Building Permits"
    verbose_name_plural = "Building Permits"
